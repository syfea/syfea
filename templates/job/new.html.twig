{% extends 'base_bo_on.html.twig' %}

{% block title %}New entreprise{% endblock %}

{% block content %}
<div class="section__content section__content--p30">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="title-3 m-b-30">Créer une entreprise</h3>
                    </div>
                    <div class="card-body">
                        {{ include('job/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <a href="{{ path('job_index') }}">back to list</a>
        </div>
    </div>
</div>
{% endblock %}
